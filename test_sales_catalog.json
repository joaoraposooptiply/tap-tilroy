{
  "streams": [
    {
      "tap_stream_id": "sales",
      "stream": "sales",
      "replication_key": "saleDate",
      "replication_method": "INCREMENTAL",
      "key_properties": ["idTilroySale"],
      "schema": {
        "type": "object",
        "properties": {
          "idTilroySale": {"type": ["string", "integer", "null"]},
          "idTenant": {"type": ["string", "integer", "null"]},
          "saleDate": {"format": "date-time", "type": ["string", "null"]},
          "totalAmountSell": {"type": ["number", "null"]},
          "shop": {"type": ["object", "string", "null"]},
          "customer": {"type": ["object", "string", "null"]},
          "lines": {"type": ["array", "null"]},
          "payments": {"type": ["array", "null"]}
        }
      },
      "metadata": [
        {
          "breadcrumb": [],
          "metadata": {
            "selected": true,
            "replication-method": "INCREMENTAL",
            "replication-key": "saleDate"
          }
        }
      ]
    }
  ]
}
